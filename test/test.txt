/*
class A {
	int a;
	void f(int x) {
		int i;
		for (i = 1; i <= x; ++i) a = a + i;
	}
};

void update(A a) {
	a.a = 10;
}

void update2(int[] a, int x) {
	a = new int[x];
	//int i = 0;
	//for (i = 0; i < x; ++i) a[i] = i;
}

int main() {
	int[] a = new int[5];
	
	update2(a, 10);
	
	int i;
	for (i = 0; i < a.size(); ++i) {
		printInt(a[i]);
		print(" ");
	}
	
	update2(a, 20);
	
	for (i = 0; i < a.size(); ++i) {
		printInt(a[i]);
		print(" ");
	}
	return 0;
	
}

int main() {
	int x = getInt(), y = getInt();
	int z = x + y;
	printInt(z);
	return 0;
}*/


class A {
	int val;
	A() {val = 1;}
};

int f(A a) {
	return a.val + 10;
}

int main() {
	A a = new A;
	printlnInt(f(a));
	return 0;
}